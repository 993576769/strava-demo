## Optional: API base URL
# 本地开发通常留空，默认通过 Vite 代理访问 http://127.0.0.1:8090。
# 当前后端不和前端同域，或你要直连远程 API 时再填写。
VITE_API_URL=

## Optional: map tiles
# 设置后活动地图会使用 MapTiler Streets 矢量底图；留空则回退到内置栅格底图。
# VITE_MAPTILER_KEY=

## Optional: Vite dev server / HMR
# 仅在 Docker、远程开发或反向代理时按需设置。
# VITE_DEV_HOST=0.0.0.0
# VITE_DEV_PORT=5173
# VITE_HMR_HOST=127.0.0.1
# VITE_HMR_PROTOCOL=ws
# VITE_HMR_CLIENT_PORT=5173
